import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.yourcompany.scpommobile',
  appName: 'SCPOMobileApp',
  webDir: 'build'
};

export default config;
